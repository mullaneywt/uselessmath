def uadd(x: int, y: int) -> int:
    return x + y


def usub(x: int, y: int) -> int:
    return x - y


def umult(x: int, y: int) -> int:
    return x * y


def udiv(x: int, y: int) -> int:
    return x / y
